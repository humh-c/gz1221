<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mygz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mygz">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Mygz.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".EquipmentPreheatingActivity"></activity>
        <activity android:name=".StartBlowActivity"></activity>
        <activity android:name=".BlowActivity"></activity>
        <activity android:name=".BlowSucessActivity"></activity>
        <activity android:name=".BlowFailActivity"></activity>
        <activity android:name=".ResultDisplayActivity"></activity>
    </application>

</manifest>